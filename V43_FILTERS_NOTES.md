# v43 filters date range

Изменения:
- Фильтры перенесены в одну строку справа от меню вкладок.
- Фильтр недели удалён.
- Добавлен фильтр периода дат через всплывающий календарь.
- Контент-план и карточка поднимаются выше за счёт уменьшения высоты зоны фильтров.
- `filteredPosts()` теперь фильтрует по `dateFrom/dateTo`, блоку, статусу и поиску.
- Состояние фильтров обновлено в `src/state/store.js`.

Проверка:
- `npm run check` проходит.
- Метка версии: `v43-filters-date-range`.
