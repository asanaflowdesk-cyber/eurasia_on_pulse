# v44 compact filters

Исправлено:
- фильтры выровнены справа от меню вкладок;
- увеличена ширина фильтра периода, чтобы диапазон дат помещался полностью;
- между фильтрами `Блок` и `Статус` задан одинаковый отступ;
- календарь периода стал компактнее;
- календарь периода теперь открывается под полем `Период`, а не уезжает вправо под карточку;
- рабочая область поднята выше за счёт уменьшения высоты панели фильтров.

Проверка:
- `npm run check` проходит.
- Метка версии: `v44-compact-filters`.
